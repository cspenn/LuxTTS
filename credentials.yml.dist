# credentials.yml.dist — template for sensitive credentials
# Copy to credentials.yml and fill in your values.
# credentials.yml is git-ignored and should never be committed.
#
# Currently LuxTTS has no credential requirements.
# Add entries here if you integrate services that require API keys.
#
# Example structure:
# huggingface:
#   token: "hf_..."
